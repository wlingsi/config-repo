server:
  port: 8888
spring:
  cloud:
    config:
      server:
        git:
          uri: https://github.com/weekly-drafts/config-repo-spring-cloud-configserver-vault
  application:
    name: configserver